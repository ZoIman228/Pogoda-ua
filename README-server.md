# Запуск локального сервера (реєстрація)

Вимагається Node.js (12+).

Команди:

```bash
cd "Pogoda-zo"
npm install
npm start
```

Після запуску відкрити http://localhost:3000
Файл бази даних створиться у `Pogoda-zo/data/users.db`.
